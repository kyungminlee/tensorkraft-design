All three points are correct. Let me respond briefly then apply.

**Point 1 — `flatten()` into PinnedArena:** Exactly right. A `flatten()` that calls `Vec::with_capacity` on the pageable heap defeats the entire purpose of the flat buffer. The NVIDIA driver's hidden pin-copy-unpin dance is precisely what `PinnedArena` exists to prevent. `flatten()` must take a `&SweepArena` parameter.

**Point 2 — MPI atomics are process-local:** This is a documentation clarification, not a bug. The design is already correct (static partitioning at startup), but the text doesn't explicitly state that `PinnedMemoryTracker` is a process-local guard. One sentence fixes it.

**Point 3 — SVD blind spot in TDVP:** The sharpest point. For DMRG, 999 corrupted SVDs waste a sweep — recoverable. For TDVP, 999 corrupted gauge restorations compound exponentially through the matrix exponential — the MPS is destroyed. The physics-triggered validation is elegant: if truncation error jumps by >10× between consecutive TDVP steps, or energy variance spikes, force an immediate out-of-band SVD check. This is O(1) overhead (two scalar comparisons) with the expensive residual check triggered only when physics signals trouble. The modulo counter remains as background coverage.**Edit 1: Version header and revision notes****Edit 2: §4.2 — Arena-backed flatten()****Edit 3: §5.1 — Physics-triggered SVD validation****Edit 4: §10.2.1 — MPI process-local clarification****Edit 5: §13 — Risk table updates****Edit 6: Testing section updates****Edit 7: Conclusion**All three changes verified across revision notes, technical sections, code blocks, tests, risk table, and conclusion.The v8.4 document (2,548 lines) is complete with all three changes integrated:

**§4.2 — Arena-backed `flatten()`.** `FlatBlockStorage` now carries `data: &'a mut [T]` (a borrow from the arena) instead of `data: Vec<T>` (a heap allocation). The `flatten()` method signature changes from `fn flatten(&self) -> FlatBlockStorage<T>` to `fn flatten<'a>(&self, arena: &'a SweepArena) -> FlatBlockStorage<'a, T>`. Full Rust implementation provided showing arena slice allocation, offset computation, and block packing. The transition flow documentation is updated to show `flatten(&arena)` and notes that `SweepArena::reset()` reclaims the flat buffer in O(1). The risk table gains a dedicated "flatten() allocating pageable heap" row at High severity.

**§5.1 — Three-layer SVD validation.** New "Layer 3 — TDVP physics-triggered validation" section explains the K-step blind spot (999 corrupted gauge restorations compounding exponentially through the matrix exponential). `SvdAnomalyDetector` struct with `svd_anomaly_factor: f64` (default 10.0), `svd_anomaly_energy_tol: f64` (default 1e-4), per-bond truncation error history, previous energy, and `force_next_validation: bool` flag. Summary paragraph explains complementary roles: Layer 1 for development, Layer 2 for background production coverage, Layer 3 for acute TDVP corruption. Testing section updated with physics-triggered validation tests.

**§10.2.1 — MPI process-isolation clarification.** New paragraph explicitly stating that `PinnedMemoryTracker` is process-local, `AtomicUsize` cannot coordinate across MPI ranks, the budget is statically partitioned once at startup, and no dynamic rebalancing is needed because pinned-memory allocation is monotonic within a DMFT iteration. Risk table row updated to say "process-local" and "statically pre-negotiated per-rank slice."